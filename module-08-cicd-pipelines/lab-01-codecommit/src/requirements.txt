# No third-party deps in the sample app — Lambda + http.server are stdlib.
# The file is here so labs 8.2 (CodeBuild) and 8.3 (CodeDeploy Lambda)
# can demonstrate `pip install -r requirements.txt` in buildspec phases.
